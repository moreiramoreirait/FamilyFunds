# ─── EXEMPLO DE VARIÁVEIS DE AMBIENTE ────────────────────────────────────────
# Copie para .env e preencha os valores reais

# ── Banco de dados ────────────────────────────────────────────────────────────
# Local (Docker):
DATABASE_URL=jdbc:postgresql://localhost:5432/familyfinance
DATABASE_USERNAME=postgres
DATABASE_PASSWORD=postgres123

# Supabase (produção) — use a "Connection string" JDBC no painel Supabase > Settings > Database:
# DATABASE_URL=jdbc:postgresql://db.xxxxxxxxxxxx.supabase.co:5432/postgres?sslmode=require
# DATABASE_USERNAME=postgres
# DATABASE_PASSWORD=SUA_SENHA_SUPABASE

# ── JWT ───────────────────────────────────────────────────────────────────────
# Gere com: openssl rand -base64 64
JWT_SECRET=bXlWZXJ5TG9uZ1NlY3JldEtleUZvckpXVFRva2VuR2VuZXJhdGlvblRoYXRJc0F0TGVhc3QyNTZCaXRzTG9uZw==
JWT_EXPIRATION=86400000
JWT_REFRESH_EXPIRATION=604800000

# ── Criptografia ──────────────────────────────────────────────────────────────
# Gere com: openssl rand -hex 16 (deve ter exatamente 32 chars)
ENCRYPTION_KEY=MySecretEncryptionKey1234567890AB

# ── CORS / Frontend ───────────────────────────────────────────────────────────
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:5174
FRONTEND_URL=http://localhost:5174

# ── E-mail (opcional — convites e alertas) ────────────────────────────────────
# Gmail: host smtp.gmail.com + "Senha de App". Brevo: host smtp-relay.brevo.com,
# username xxxx@smtp-brevo.com, password = SMTP key.
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=seu@gmail.com
MAIL_PASSWORD=xxxx_xxxx_xxxx_xxxx
# Remetente. OBRIGATÓRIO no Brevo: precisa ser um sender VERIFICADO na conta.
MAIL_FROM=naoresponda@seudominio.com

# ── Porta ────────────────────────────────────────────────────────────────────
PORT=8080
